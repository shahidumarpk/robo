<?php
/*
// Home
Breadcrumbs::for('dashboard', function ($trail) {
    $trail->add('Home', route('dashboard'));

});
//Manage admins and roles
Breadcrumbs::for('roles', function ($trail) {
    $trail->parent('dashboard');
    $trail->add('Manage Admin Roles', route('roles'));
});
Breadcrumbs::for('admins', function ($trail) {
    $trail->parent('dashboard');
    $trail->add('Manage Admins', route('admins'));
});

//Change Password
Breadcrumbs::for('changepassword', function ($trail) {
    $trail->parent('dashboard');
    $trail->add('Change Password', route('changepassword'));
});
//Profile
Breadcrumbs::for('profile', function ($trail) {
    $trail->parent('dashboard');
    $trail->add('Profile', route('profile'));
});*/
